import { StyleSheet } from "react-native";

const NAVBAR_HEIGTH = 50;

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  
  listContainer: {
    flex: 1,
    backgroundColor: "#999",
   },
});
